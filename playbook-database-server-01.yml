---
- hosts: dbserver 
  roles:
    - common
    - role: postgres
      vars:
        postgres_version: 15
        ubuntu_distro: jammy #this needs to match lsb_release -cs command output
  become: yes

